export class Point {
   x: number;
   y: number;
   rad: number;
   color: string;

   constructor(x: number, y: number, rad: number,color: string) {
    this.x = x;
    this.y = y;
    this.rad = rad;
    this.color = color;
  }

}

